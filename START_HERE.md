# دليل البدء السريع — تطبيق تسجيل الحضور (تعليمات الترعة)

نظام بسيط: صفحة ويب فيها زرار "حضور" و"انصراف"، كل موظف يفتحها من موبايله،
والبيانات بتتخزن مجانًا وتتحول لتقرير Excel بساعات العمل الأسبوعية.

**التكلفة: صفر جنيه. مفيش كارت ائتمان مطلوب في أي خطوة.**

---

## الترتيب الصحيح للتنفيذ

نفّذ الملفات دي بالترتيب ده بالظبط:

### 1️⃣ أولاً: `FIREBASE_SETUP.md`
تنشئ فيه قاعدة بيانات مجانية تخزن تسجيلات الحضور. هتاخد منه 6 قيم (API keys)
هتحتاجها في الخطوة الجاية.

### 2️⃣ ثانيًا: عدّل ملفي `index.html` و `report.html`
حط القيم اللي أخدتها من Firebase مكان `YOUR_API_KEY` وباقي القيم المشابهة
(موضح بالتفصيل جوه دليل Firebase نفسه).

### 3️⃣ ثالثًا: `GITHUB_PAGES_SETUP.md`
ترفع فيه الملفين على GitHub، وتاخد رابط عام تقدر تبعته لكل الموظفين.

---

## بعد ما تخلّص الثلاث خطوات

- **صفحة الموظفين:** `https://YOUR-USERNAME.github.io/attendance-terra/`
  (تبعتها لكل الموظفين مرة واحدة، ويفتحوها يوميًا)

- **صفحة التقرير:** `https://YOUR-USERNAME.github.io/attendance-terra/report.html`
  (تحتفظ بيها لنفسك، فيها زرار تصدير Excel)

- **صفحة إدارة الموظفين:** `https://YOUR-USERNAME.github.io/attendance-terra/admin.html`
  (محمية بكلمة سر، منها تضيف أو تمسح أي اسم موظف)

---

## ملفات المشروع

| الملف | الوظيفة |
|---|---|
| `index.html` | الصفحة اللي بيفتحها الموظفين لتسجيل الحضور/الانصراف |
| `report.html` | صفحة التقرير الأسبوعي + تصدير Excel |
| `admin.html` | صفحة إدارة أسماء الموظفين (إضافة/حذف)، محمية بكلمة سر |
| `FIREBASE_SETUP.md` | دليل إنشاء قاعدة البيانات المجانية |
| `GITHUB_PAGES_SETUP.md` | دليل رفع الموقع مجانًا |

---

## أسئلة شائعة

**س: هل الموظف محتاج يثبت تطبيق؟**
لأ، مجرد رابط يفتحه من أي متصفح (Chrome, Safari). ممكن يضيفه لشاشته الرئيسية
عشان يبان زي تطبيق (مشروح في دليل GitHub Pages).

**س: هل محتاج إنترنت؟**
أيوه، وقت الضغط على الزرار بس (بيانات بسيطة جدًا، أقل من رسالة واتساب).

**س: لو الموظف نسي يضغط "انصراف"؟**
صفحة التقرير هتحسبله 8 ساعات افتراضية (نفس المنطق اللي اتفقنا عليه قبل كده)
وهتعلّمها بالتظليل عشان تراجعها لو حبيت.

**س: هل أي حد يقدر يدخل يسجل حضور مكان زميله؟**
النظام الحالي بسيط ومفيهوش تحقق هوية (متوافق مع اختيارك "زرار بس بدون قيود").
لو حبيت لاحقًا تضيف حماية (كلمة سر بسيطة لكل موظف مثلاً)، قولي ونضيفها.

**س: هل ينفع أستخدم نفس الفكرة مع أكتر من جروب أو مزرعة؟**
أيوه، تقدر تكرر نفس الخطوات بمشروع Firebase ومستودع GitHub منفصلين لكل مكان.

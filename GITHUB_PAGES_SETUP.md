# دليل النشر على GitHub Pages — خطوة بخطوة

بعد ما تظبط إعدادات Firebase (ملف `FIREBASE_SETUP.md`)، الخطوة دي هتخلي التطبيق
متاح على رابط عام يقدر أي موظف يفتحه من موبايله.

---

## الخطوة 1: إنشاء حساب GitHub (لو معندكش)

1. روح على: https://github.com
2. اضغط **Sign up** وسجّل بإيميلك (مجاني تمامًا، بدون كارت)

---

## الخطوة 2: إنشاء مستودع (Repository) جديد

1. بعد تسجيل الدخول، اضغط الزرار **+** في الأعلى ← **New repository**
2. اسم المستودع: اكتب `attendance-terra` (أو أي اسم بالإنجليزي بدون مسافات)
3. اختار **Public**
4. **متعملش تفعيل** أي من الخيارات التانية (README, .gitignore, license)
5. اضغط **Create repository**

---

## الخطوة 3: رفع ملفات التطبيق

### الطريقة الأسهل (بدون أي أوامر Terminal):

1. في صفحة المستودع الفاضية، هتلاقي رابط **"uploading an existing file"**
2. اسحب وأفلت (Drag & Drop) الملفين:
   - `index.html`
   - `report.html`
   (بعد ما تكون عدّلت فيهم إعدادات Firebase زي ما اتشرح في الدليل التاني)
3. اكتب رسالة بسيطة في خانة **"Commit changes"** زي "رفع التطبيق"
4. اضغط **Commit changes**

---

## الخطوة 4: تفعيل GitHub Pages

1. من صفحة المستودع، روح لـ **Settings** (فوق على اليمين)
2. من القائمة الجانبية، اضغط **Pages**
3. تحت **"Build and deployment"** ← **Source**، اختار **"Deploy from a branch"**
4. تحت **Branch**، اختار **main** والمجلد **/ (root)**
5. اضغط **Save**

استنى دقيقة أو دقيقتين، وارجع لنفس الصفحة (Settings ← Pages) هتلاقي رسالة خضرا فيها
رابط شبه ده:

```
https://YOUR-USERNAME.github.io/attendance-terra/
```

---

## الخطوة 5: اختبار الرابط

1. افتح الرابط من أي موبايل
2. المفروض تشوف صفحة تسجيل الحضور جاهزة
3. جرب تختار اسم وتضغط "حضور" — لو ظهرت رسالة "تم التسجيل بنجاح"، كل حاجة شغالة

للتأكد من التخزين فعليًا:
1. ارجع للوحة تحكم Firebase (console.firebase.google.com)
2. روح لـ **Firestore Database**
3. المفروض تلاقي مجموعة اسمها `attendance_events` فيها التسجيل اللي عملته

---

## الخطوة 6: عرض التقرير

رابط صفحة التقرير هيبقى:
```
https://YOUR-USERNAME.github.io/attendance-terra/report.html
```

احتفظ بالرابط ده لنفسك (أو لأي حد مسؤول عن المتابعة) — فيه زرار "تصدير Excel"
يطلعلك ملف فيه ساعات كل موظف أسبوعيًا.

---

## توزيع الرابط على الموظفين

ابعت رابط `index.html` لكل الموظفين (عبر واتساب مثلاً، مرة واحدة بس):

> افتح الرابط ده كل يوم لما توصل الشغل ولما تمشي، واضغط الزرار المناسب:
> https://YOUR-USERNAME.github.io/attendance-terra/

نصيحة: قولهم يضيفوا الرابط لشاشة موبايلهم الرئيسية (زي تطبيق) عشان يبقى سهل الوصول:
- **أندرويد (Chrome):** افتح الرابط ← القائمة (⋮) ← "إضافة إلى الشاشة الرئيسية"
- **آيفون (Safari):** افتح الرابط ← زرار المشاركة (□↑) ← "إضافة إلى الشاشة الرئيسية"

---

## تحديث قائمة الموظفين لاحقًا

لو عايز تضيف أو تشيل موظف:
1. من صفحة المستودع على GitHub، افتح ملف `index.html`
2. اضغط أيقونة القلم (✎) للتعديل
3. دور على السطر:
   ```javascript
   const EMPLOYEES = ["يويو", "Hagoraa", "Nermin", ...];
   ```
4. عدّل الأسماء زي ما تحب
5. اضغط **Commit changes** في الأسفل

التغيير هيظهر على الرابط تلقائيًا خلال دقيقة أو اتنين.
